# Variables de entorno para el servicio de Inventario Deacero
# Copia este archivo a ".env" y ajusta los valores según tu entorno.
#
# Uso local:
#   set -a; source .env; set +a
#   ./mvnw spring-boot:run
#
# Uso con Docker:
#   docker run --env-file .env -p 8080:8080 deacero/inventario:latest
#
# -----------------------------------------------------------------------------

# Puerto HTTP del servicio
SERVER_PORT=8080
SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/deacero_inventory_db
SPRING_DATASOURCE_USERNAME=deacerouser1
SPRING_DATASOURCE_PASSWORD=Password123#


